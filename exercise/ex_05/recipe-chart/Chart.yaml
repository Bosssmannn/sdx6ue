apiVersion: v2
name: recipe-api
description: Helm chart for the Recipe API with PostgreSQL dependency
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "15.5.38"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
